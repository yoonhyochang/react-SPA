import React from "react"; // React 라이브러리 불러오기

// ScreenB라는 이름의 함수형 컴포넌트 정의
const ScreenB = () => {
  return <div>ScreenB</div>; // "ScreenB"라는 텍스트를 포함한 div 엘리먼트 반환
};

export default ScreenB; // ScreenB 컴포넌트를 기본으로 내보내기
