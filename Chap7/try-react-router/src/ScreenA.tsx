import React from "react"; // React 라이브러리 불러오기

// ScreenA라는 이름의 함수형 컴포넌트 정의
const ScreenA = () => {
  return <div>ScreenA</div>; // "ScreenA"라는 텍스트를 포함한 div 엘리먼트 반환
};

export default ScreenA; // ScreenA 컴포넌트를 기본으로 내보내기
